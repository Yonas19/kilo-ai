import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function profile() {
  return (
    <View>
      <Text className="flex-1 bg-green">profile</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
